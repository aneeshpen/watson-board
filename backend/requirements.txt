# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.103.1
uvicorn>=0.23.2
python-multipart>=0.0.6
pydantic>=2.3.0
pydantic-settings>=2.0.0
slowapi>=0.1.9

# ── Data / ML ────────────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.24.0
scikit-learn>=1.4.0
joblib>=1.3.0
shap>=0.44.0

# ── Vector search ────────────────────────────────────────────────────────────
# Pinned <2: backend/chroma_data is a committed binary store; a major
# version bump could change its on-disk schema.
chromadb>=1.0.0,<2.0.0

# ── Copilot (RAG generation) ─────────────────────────────────────────────────
anthropic>=0.40.0

# ── Computer vision ──────────────────────────────────────────────────────────
# Pinned <5: OpenCV 5.x removed cv2.HOGDescriptor, which cctv_analyzer uses.
opencv-python-headless>=4.8.0,<5.0.0
Pillow>=10.0.0
